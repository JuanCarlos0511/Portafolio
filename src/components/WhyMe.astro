---
// WhyMe.astro - Sección "Por qué elegirme"
const features = [
  {
    icon: '�',
    title: 'Team Collaboration',
    description: 'Experience in collaborative environments, participating in academic and professional projects that require effective coordination and communication.'
  },
  {
    icon: '�',
    title: 'Effective Communication',
    description: 'Ability to convey ideas clearly and concisely, facilitating team collaboration and understanding of client requirements.'
  },
  {
    icon: '🎯',
    title: 'Client Oriented',
    description: 'Commitment to client satisfaction, ensuring their needs and expectations are met effectively with quality solutions.'
  },
  {
    icon: '�',
    title: 'Production Ready',
    description: 'Experience developing projects taken to production, including point of sale systems and functional web applications for real clients.'
  }
];

const languages = [
  {
    name: 'Español',
    level: 'Native',
    flag: '🇪🇸',
    gradient: 'linear-gradient(135deg, #ff0000, #f1ff1d)'
  },
  {
    name: 'English',
    level: 'Competent', 
    flag: '🇺🇸',
    gradient: 'linear-gradient(135deg, #0400ff, #ffffff)'
  }
];
---

<section id="sobre-mi" class="why-me">
  <div class="why-me-content">
    <div class="why-me-text">
      <h2>Why me?</h2>
      <p>
        Developer since age 15 with a technical programming background from CBTIS No. 103. Currently pursuing Systems Engineering at Universidad Autónoma de Tamaulipas while working on real production projects. My combination of academic knowledge and practical experience allows me to deliver quality solutions efficiently.
      </p>
      
      <!-- Sección de idiomas -->
      <div class="languages-section">
        <h3>Languages I speak</h3>
        <div class="languages-grid">
          {languages.map((language) => (
            <div class="language-item" style={`--language-gradient: ${language.gradient}`}>
              <span class="language-flag">{language.flag}</span>
              <div class="language-info">
                <span class="language-name">{language.name}</span>
                <span class="language-level">{language.level}</span>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
    
    <div class="features-grid">
      {features.map((feature) => (
        <div class="feature-card">
          <div class="feature-icon">
            <span>{feature.icon}</span>
          </div>
          <div class="feature-content">
            <h3>{feature.title}</h3>
            <p>{feature.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
.why-me {
  padding: 6rem 0;
  background: rgba(255, 255, 255, 0.05);
  position: relative;
}

.why-me-content {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 4rem;
  align-items: start;
}

.why-me-text h2 {
  font-size: 3rem;
  font-weight: normal;
  color: var(--text-light);
  margin-bottom: 1.5rem;
  font-family: var(--font-titles);
  letter-spacing: 0.02em;
  background: linear-gradient(135deg, var(--text-light) 0%, var(--accent-blue) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.why-me-text p {
  font-size: 1.1rem;
  color: var(--text-muted);
  line-height: 1.7;
  margin-bottom: 2rem;
  font-family: var(--font-content);
}

/* Sección de idiomas */
.languages-section {
  margin-top: 2rem;
}

.languages-section h3 {
  color: var(--text-light);
  font-size: 1.5rem;
  font-weight: normal;
  margin-bottom: 1rem;
  text-align: center;
  font-family: var(--font-titles);
  letter-spacing: 0.01em;
}
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: 1.5rem;
}

.languages-grid {
  display: flex;
  gap: 2rem;
  flex-wrap: wrap;
  justify-content: center;
}

.language-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 15px;
  padding: 1rem 1.5rem;
  transition: all 0.3s ease;
  backdrop-filter: blur(10px);
  min-width: 200px;
  position: relative;
  overflow: hidden;
}

.language-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--language-gradient);
  opacity: 0.15;
  transition: opacity 0.3s ease;
  z-index: -1;
}

.language-item:hover {
  background: rgba(255, 255, 255, 0.08);
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
  border-color: rgba(255, 255, 255, 0.2);
}

.language-item:hover::before {
  opacity: 0.3;
}

.language-item:hover .language-flag {
  transform: scale(1.1);
}

.language-item:hover .language-name {
  background: var(--language-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.language-flag {
  font-size: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  flex-shrink: 0;
  transition: all 0.3s ease;
}

.language-info {
  display: flex;
  flex-direction: column;
  gap: 0.2rem;
}

.language-name {
  color: var(--text-light);
  font-weight: 600;
  font-size: 1.1rem;
  transition: all 0.3s ease;
  font-family: var(--font-content);
}

.language-level {
  color: var(--text-muted);
  font-size: 0.9rem;
  font-weight: 500;
  transition: color 0.3s ease;
  font-family: var(--font-content);
}

.features-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 2rem;
}

.feature-card {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 15px;
  padding: 1.5rem;
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  transition: all 0.3s ease;
  backdrop-filter: blur(10px);
  min-height: 120px;
}

.feature-card:hover {
  background: rgba(255, 255, 255, 0.08);
  transform: translateY(-2px);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.feature-icon {
  width: 60px;
  height: 60px;
  background: linear-gradient(135deg, var(--accent-blue), var(--light-blue));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  margin-top: 0.2rem;
}

.feature-icon span {
  font-size: 1.5rem;
}

.feature-content {
  display: flex;
  flex-direction: column;
  justify-content: center;
  flex-grow: 1;
  min-height: 60px;
}

.feature-card h3 {
  color: var(--text-light);
  font-size: 1.2rem;
  font-weight: normal;
  margin-bottom: 0.5rem;
  line-height: 1.3;
  font-family: var(--font-titles);
  letter-spacing: 0.01em;
}

.feature-card p {
  color: var(--text-muted);
  font-family: var(--font-content);
  line-height: 1.6;
}
  font-size: 0.95rem;
  line-height: 1.5;
  margin: 0;
}

/* Responsive */
@media (max-width: 1024px) {
  .why-me-content {
    grid-template-columns: 1fr;
    gap: 3rem;
  }
  
  .why-me-text {
    text-align: center;
  }
}

@media (max-width: 768px) {
  .why-me {
    padding: 4rem 0;
  }
  
  .why-me-content {
    padding: 0 1rem;
  }
  
  .why-me-text h2 {
    font-size: 2.5rem;
  }
  
  .feature-card {
    flex-direction: column;
    text-align: center;
    gap: 1rem;
  }
  
  .feature-icon {
    align-self: center;
  }
  
  /* Responsive para idiomas */
  .languages-grid {
    flex-direction: column;
    gap: 1.5rem;
  }
  
  .language-item {
    min-width: auto;
    justify-content: center;
  }
  
  .languages-section h3 {
    font-size: 1.3rem;
  }
}
</style>
